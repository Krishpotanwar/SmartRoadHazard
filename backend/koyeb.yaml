name: smartroadhazard
services:
  - name: api
    type: web
    github:
      branch: main
      buildCommand: pip install -r requirements.txt
      runCommand: gunicorn server:app --bind 0.0.0.0:$PORT --workers 1
    routes:
      - path: /
    ports:
      - port: 8000
        protocol: http
